console.log(1+4);
let signal ='yeloow';
console.log(`the signal is `);
if(signal == 'red'){
    console.log(`the signal is ${signal} STOP`);
}if(signal == 'yellow'){
    console.log(`the signal is ${signal} : GO SLOW`)
}else
{
    console.log(`the signal is ${signal} : GO`)
};